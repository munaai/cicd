name: My first CICD workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: checkout code
      uses: actions/checkoutv2
    - name: We are testing this workflow
      run: echo Heloo everyone  
